const db = require('../../data/db-config.js');

const getAll = () => {
  return null;
}

const getById = (id) => {
  return null;
}

const insert = async (student) => {
  return null;
}

const update = async (id, student) => {
  return null;
}

const remove = (id) => {
  return null;
}

module.exports = {
  getAll,
  getById,
  insert,
  update,
  remove
}